# Protótipos

Sandbox do Nico. Cada spike vive em `prototipos/<nome-curto>/` com um
`CONCLUSAO.md` de 1 página (formato no skill `prototipo-relampago`).

Tudo aqui é descartável por definição — promover a projeto real exige
aprovação em `escritorio/aprovacoes.md`.
